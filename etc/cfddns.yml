token: "<CloudFlare API token>" # required
# endpoint: "https://api.ipify.org" # (default: "https://api.ipify.org")
# interval: 300 # in seconds (default: 300)
# notification:
#   enable: true # default: false
#   from: cfddns@localhost
#   to: admin@example.com
